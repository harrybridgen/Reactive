import evaluator.tokenizer;
import evaluator.parser;
import std.input;


func main() {
    println "Enter expression:";
    src := input_readline();

    lexer := tokenize(src);
    ast := parse(lexer.tokens,lexer.len);
    println ast.value;
}
